# Generated by Cargo
# will have compiled files and executables
debug/
target/


# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
.DS_Store

config.json

# Ignore local signer keystore files everywhere except for the integration tests directory
**/local-signer.json
**/config/keys/*.json
!tests/integration/config/**/*.json
tests/integration/config/keys/*.json

# Integration test config files (git-ignored, use .example files as templates)
tests/integration/config/config.json
tests/integration/config/registry.json

!examples/**/*.json


# docs
docs/build/
docs/yarn.lock
rust_docs

# logs
logs/

# env
.env
.env.integration

# Ignore node_modules
node_modules
.qodo


# Exclude generated OpenAPI files from the docs directory
!./openapi.json

# Integration test artifacts
test-results/
**/*lcov.info
