# Integration Test Environment Variables
# Copy this file to .env.integration and fill in your values

# ==============================================================================
# Required Variables
# ==============================================================================

# API key for the relayer service (must match relayer's API_KEY)
RELAYER_API_KEY=your-api-key-here

# Keystore passphrase for local signer
KEYSTORE_PASSPHRASE=your-keystore-passphrase-here

# Webhook signing key (can be any UUID)
WEBHOOK_SIGNING_KEY=your-webhook-signing-key-here

# ==============================================================================
# Test Configuration
# ==============================================================================

# Networks to test (comma-separated list)
# Examples:
#   - Single network: TEST_NETWORKS=sepolia
#   - Multiple networks: TEST_NETWORKS=sepolia,binance-smart-chain-testnet
#   - All EVM testnets: TEST_NETWORKS=* (with TEST_TAGS=evm,testnet)
TEST_NETWORKS=sepolia

# Optional: Filter networks by tags instead of explicit names
# Example: TEST_TAGS=evm,quick
# TEST_TAGS=

# Test mode: quick (1-2 networks), ci (essential networks), full (all networks)
# TEST_MODE=quick

# ==============================================================================
# Optional Configuration
# ==============================================================================

# Relayer API URL (automatically set in docker-compose, override if needed)
# RELAYER_API_URL=http://relayer:8080/api/v1
