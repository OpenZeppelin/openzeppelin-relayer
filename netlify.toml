[build]
base = "/"
command = "rustup toolchain install 1.88.0 && rustup default 1.88.0 && cargo install cargo-make --locked --force && cargo make rust-docs"
publish = "docs/build/site"

[build.environment]
PKG_CONFIG_PATH = "/home/linuxbrew/.linuxbrew/lib/pkgconfig"
RUST_TOOLCHAIN = "1.88.0"
